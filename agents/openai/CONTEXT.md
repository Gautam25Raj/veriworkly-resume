# 🤖 OpenAI-Specific Context

When working on VeriWorkly with OpenAI (GPT-4o):

1. **Logic Verification**: GPT-4o is excellent for debugging complex state management in our Zustand stores (`apps/resume-builder/store`).
2. **API Design**: Use it to review and optimize our Express controllers and services in `apps/server/src`.
3. **Regex & Utilities**: Highly efficient for writing the transformation utilities in `apps/resume-builder/utils`.
